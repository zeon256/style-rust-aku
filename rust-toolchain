[toolchain]
# Must match the nightly declared by the pinned clippy_utils rev
# (rust-clippy/rust-toolchain.toml at that rev) — rustc_private APIs
# only line up for that exact date.
channel = "nightly-2026-07-09"
components = ["llvm-tools-preview", "rustc-dev", "rust-src"]
